<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yxf.demo.mapper.RecommendMapper">
    <select id="loadByIdList" resultType="com.yxf.demo.entity.Movie">
        select m.id as "movieId", m.movie_name as "movieName", m.img_path as "imgPath",
        m.movie_time as "time", m.brief as "brief", m.score as "stars", t.type_name as "type"
        from movie m
        left join type t on m.type_id = t.id
        where m.id = #{id};
    </select>
</mapper>